// Debug configurations — select in the debug dropdown (Ctrl+Shift+D), then press F5.
// Requires the C/C++ extension (ms-vscode.cpptools).
{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Local: Debug",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/build/debug/warden",
      "args": ["--config", "${workspaceFolder}/config/config.cfg"],
      "cwd": "${workspaceFolder}",
      "stopAtEntry": false,
      "externalConsole": false,
      "MIMode": "gdb",
      "miDebuggerPath": "/usr/bin/gdb",
      "preLaunchTask": "Local: Build",
      "setupCommands": [
        {
          "description": "Enable pretty-printing",
          "text": "-enable-pretty-printing",
          "ignoreFailures": true
        }
      ]
    },
    {
      "name": "Pi: Remote Debug",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/build/pi-debug/warden",
      "args": [],
      "cwd": "${workspaceFolder}",
      "stopAtEntry": false,
      "externalConsole": false,
      "MIMode": "gdb",
      // gdb-multiarch is installed in the devcontainer
      "miDebuggerPath": "/usr/bin/gdb-multiarch",
      "miDebuggerServerAddress": "${env:PI_HOST}:2345",
      "targetArchitecture": "arm64",
      "preLaunchTask": "Pi: Start gdbserver",
      "setupCommands": [
        {
          "description": "Enable pretty-printing",
          "text": "-enable-pretty-printing",
          "ignoreFailures": true
        }
      ]
    }
  ]
}
